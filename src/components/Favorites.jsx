import { useParams } from "react-router-dom";


const { characterId } = useParams;      // da acceso a los parámetros de la URL (como el id del contacto).

// 	const handleClick = () => {
	// store.characters.map((element, idx) => {
		
	// }

//     // Lógica a ejecutar cuando se hace clic
//     console.log("¡Botón clickeado!");
//   };
//   return (
//     <button onClick={handleClick}>
//       Haz clic aquí
//     </button>
//   );